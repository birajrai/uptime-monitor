services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=${DATABASE_URL}
      - APP_SECRET=${APP_SECRET}
      - CRON_API_KEY=${CRON_API_KEY}
    restart: unless-stopped
